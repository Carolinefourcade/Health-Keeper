<form method="post" action="{{ path('annoyance_delete', {'id': annoyance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ annoyance.id) }}">
    <button class="btn">Delete</button>
</form>
