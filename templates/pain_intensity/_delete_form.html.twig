<form method="post" action="{{ path('pain_intensity_delete', {'id': pain_intensity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pain_intensity.id) }}">
    <button class="btn">Delete</button>
</form>
