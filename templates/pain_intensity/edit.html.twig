{% extends 'base.html.twig' %}

{% block title %}Edit PainIntensity{% endblock %}

{% block body %}
    <h1>Edit PainIntensity</h1>

    {{ include('pain_intensity/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('pain_intensity_index') }}">back to list</a>

    {{ include('pain_intensity/_delete_form.html.twig') }}
{% endblock %}
