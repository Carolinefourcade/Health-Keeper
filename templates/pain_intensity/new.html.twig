{% extends 'base.html.twig' %}

{% block title %}New PainIntensity{% endblock %}

{% block body %}
    <h1>Create new PainIntensity</h1>

    {{ include('pain_intensity/_form.html.twig') }}

    <a href="{{ path('pain_intensity_index') }}">back to list</a>
{% endblock %}
