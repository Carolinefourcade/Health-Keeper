{% extends 'base.html.twig' %}

{% block title %}AnnoyanceZone{% endblock %}

{% block body %}
    <h1>AnnoyanceZone</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ annoyance_zone.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ annoyance_zone.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('annoyance_zone_index') }}">back to list</a>

    <a href="{{ path('annoyance_zone_edit', {'id': annoyance_zone.id}) }}">edit</a>

    {{ include('annoyance_zone/_delete_form.html.twig') }}
{% endblock %}
